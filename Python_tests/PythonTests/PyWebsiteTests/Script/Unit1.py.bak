def EventControl1_OnStartTest(Sender):
  
  CONST_INPUT = "Input"
  
  Log.Message("Start")

  found = False
  i = 1
  while (i <= BuiltIn.ParamCount() and not found):
    parm = BuiltIn.ParamStr(i);
    
    p = aqString.Find(parm, "Data", 0, true);

    found = p == 0;
    
    if (found):
      parts = parm.split("=");
      if not Project.Variables.VariableExists(INPUT):
          Project.Variables.AddVariable(INPUT, "String")

       Project.Variables.Item["VariableByName", CONST_INPUT, parts[1]]
       
    i+=1
    
